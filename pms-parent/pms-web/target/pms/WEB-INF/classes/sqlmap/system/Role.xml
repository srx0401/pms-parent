<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.srx.pms.system.mapper.RoleMapper">
    <resultMap id="roleResultMap" type="Role" extends="com.srx.pms.common.mapper.BaseMapper.baseEntityResultMap">
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="desc" property="desc" jdbcType="VARCHAR"/>
   		<collection property="permissions" ofType="Permission"
               javaType="java.util.ArrayList" select="com.srx.pms.system.mapper.PermissionMapper.selectByRoleId"
               column="id">
        </collection>
        <collection property="resources" ofType="Resource"
               javaType="java.util.ArrayList" select="com.srx.pms.system.mapper.ResourceMapper.selectByRoleId"
               column="id">
        </collection>
    </resultMap>
	<sql id="role_column">
        id,
        name,
        desc,
        valid,
        remark,
        create_time,
        create_user_id,
        update_time,
        update_user_id
    </sql>
	<sql id="role_column_where">
        <if test="id !=null ">
            AND T.id = #{id}
        </if>
        <if test="name !=null and name.length() != 0">
            AND T.name LIKE concat('%', #{name},'%')
        </if>
        <if test="valid !=null ">
            AND T.valid = #{valid}
        </if>
        <if test="createTimeBegin !=null ">
            AND T.create_time &gt;= #{createTimeBegin}
        </if>
        <if test="createTimeEnd !=null ">
            AND T.create_time &lt;= #{createTimeEnd}
        </if>
        
        <if test="createUserId !=null ">
            AND T.create_user_id = #{createUserId}
        </if>
        <if test="updateUserId !=null ">
            AND T.update_user_id = #{updateUserId}
        </if>
    </sql>
	<insert id="insert" parameterType="Role">
    	<selectKey keyProperty="id" resultType="java.lang.String"
			order="BEFORE">
			select replace(upper(uuid()),'-','') from dual
		</selectKey>
        insert into pms_role (<include refid="role_column"></include>)
        values(
        	#{id},
	        #{name},
	        #{desc},
	        #{valid},
	        #{remark},
	        #{createTime},
	        #{createUserId},
	        #{updateTime},
	        #{updateUserId}
	        )
    </insert>

    <update id="update" parameterType="Role">
        update pms_role
        <set>
            id=#{id}
            <if test="name !=null and name.length() !=0"> ,name = #{name} </if>
            <if test="desc !=null"> ,desc = #{desc} </if>
            <if test="valid !=null"> ,valid = #{valid} </if>
            <if test="remark !=null"> ,remark = #{remark} </if>
            <if test="updateTime !=null"> ,update_time = #{updateTime} </if>
            <if test="updateUserId !=null"> ,update_user_id = #{updateUserId} </if>
        </set>
        where id =#{id}
    </update>
    <update id="delete" parameterType="String">
        UPDATE pms_role SET valid = 0 WHERE id = #{id}
    </update>
	<update id="batchDelete" parameterType="java.util.List">
        UPDATE pms_role SET valid = 0 WHERE id in
        <foreach collection="ids" index="index" item="item" open="(" separator="," close=")">
            #{item}       
   		</foreach> 
    </update>
    <select id="select" parameterType="RoleQueryBean" resultMap="roleResultMap">
        select * from pms_role T
        WHERE 1=1 
        <include refid="role_column_where"></include>
        ORDER BY #{order}
    </select>
    <select id="selectByUserId" parameterType="String" resultMap="roleResultMap">
        select T.* from pms_role T,pms_user A,pms_user_role B
        WHERE T.id = B.role_id AND A.id = B.user_id
        AND A.id = #{userId}
        ORDER BY #{order}
    </select>
    <select id="load" parameterType="String" resultMap="roleResultMap">
        SELECT * FROM pms_role WHERE id = #{id}
    </select>
    <select id="count" resultType="long" parameterType="RoleQueryBean">
        SELECT count(1) FROM pms_role T
        WHERE 1=1 
        <include refid="role_column_where"></include>
    </select>
</mapper>